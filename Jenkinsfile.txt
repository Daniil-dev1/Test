pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'sudo su jenkins'
                sh '/var/lib/jenkins/workspace/GitPipe/Hello_test.cpp'
            }
        }
    }
}
